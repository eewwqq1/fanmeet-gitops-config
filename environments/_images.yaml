# Auto-bumped by the fanmeet-app-source Jenkins pipeline's "Bump gitops-config
# tags" stage on every build that changes a service (same commit also bumps
# the matching tag in environments/onprem/values-<svc>.yaml and
# environments/aws/values-<svc>.yaml). Manual edits will just be overwritten
# by the next build — this file is a tag-per-service reference, not the
# source Argo CD deploys from (that's the per-environment values files).
server: "83dc9ad"
media: local
notification: "cd74154"
membership-billing: "cd74154"
